# Stage 1：暂停的 L100 与地图约束修复

2026-09-10。结论：`a2f780b` 工程验证通过，但真实 L100 在接纳第 2 章后发现明确时间/移动冲突，已安全暂停。百章、全书结尾和生产切换均未通过；旧样本保留原状。

## 冻结身份与实际运行

| 项目 | 身份或结果 |
| --- | --- |
| 源码 | `a2f780b459a6dcfa6f24b24f6c5f0eb83a6bf310` |
| 树 | `b09e09b065974673500798881f0249c4a2729cb6` |
| 运行镜像 | `sha256:adfdf4d2b6e4d1e8a8873473ee0edee805eafce18f1a4505549b24aa2b9904cd` |
| 浏览器镜像 | `sha256:92e0f879178618f2c6e1d3ade822b0e59aac110427eff9f0e5d44b7c9690d358` |
| 工程回归 | 3063 passed、4 skipped、6 subtests，359.78 秒；compile/F/E9/源码守卫通过 |
| 完整 Ruff | 1119，原始基线 1219，路径/诊断码/消息多重集无新增；非全库零告警 |
| 隔离项目 / 任务 | `d45a8fb18cdc4fb592c263db1ffe3674` / `e022cbdf8cf5` |
| Genesis / 策略 | 六阶段锁定 revision 15；policy 2，只变更三个已支持的自动暂停/委托字段 |
| 目标 / 终态 | `run_until_chapter=100`；安全暂停，accepted 1–2；第 3 章未接纳 |

冻结归档及两种镜像已核对生产源码、依赖锁、完整 revision、迁移 head 和角色导入；浏览器在无网络容器实际启动并执行 DOM/JS，包含 Xvfb 路径。隔离运行没有生产卷、主机端口或真实发布角色。模型目录未改变；两家供应商因余额限制落入现有 Codex Bridge 路径，这属于本次真实环境的限制，不代表所有供应商均完成实跑。

暂停只通过一次受支持的 `task_pause` 请求。只读观察器在 `2026-09-10T07:48:39.416639Z` 确认 paused，并完整导出两章、任务、项目、cost/gates；导出前后 book revision 一致。200 条事件窗口已饱和，不把它当完整事件历史。

## 接纳正文中的失败

第 2 章版本 2，经历一次修复，正常接纳且 residual 列表为空。正式正文 SHA256：`a5217c4f8f5c22ebfe4fddf0ad62235858d3cd7c6db4e8035afa0c01065a1766`。

| 正文时间 / 地点 | 锁定地图 | 判断 |
| --- | --- | --- |
| 11:47 旧港联检楼 → 11:49 港务档案库 | 行程 10 分钟，另有门禁登记等待 | 两分钟不足以完成基础行程 |
| 11:57 仍在档案库 → 11:57 到财务结算室 | 行程 15 分钟，手续另计 | 同一分钟完成移动与设定冲突 |

主审读的是最终正式 BODY。scene 元数据包含“旧港联检楼至港务档案库”“港务档案库核验台”等自由文本位置；时间抽取写为“从十一点四十七分推进至十二点，历时十三分钟。”。结构化信息与接纳成功都没有证明这两处正文移动正确。

## 原因与修复证据

从 canonical Genesis/BODY 和明确 artifact URI 只读复制原始输入，先校验 BODY hash，再在原始冻结归档和新实现分别运行同一纯函数脚本，核对实际导入源文件及 SHA。

| 实际输入的回放 | 原 `a2f780b` | 本次修复 |
| --- | --- | --- |
| 两条地点间路线 | 丢弃明确局部边，重建为各 0.01 小时的 `portal` | 保留各 10、15 分钟的 `path` |
| “门禁”语义 | “门”触发传送阵推断 | 不从门禁文字推断超自然交通 |
| Genesis Writer 总览 | 没有 10/15 分钟路线约束 | 保留原始路线及手续说明 |
| BODY reviewer 输入 | `world` 缺失地图与地图总览 | 输入当前地图和原始设定证据 |
| 明确“历时十三分钟” | 解析为未知 | 13/60 小时 |
| 两个复合自由文本地点 | 不能解析 | 仍未知，未猜测位置或改写旧产物 |

路线只在现有 BookMap 入口规范化一次，局部与跨区持久化共用原始地点、方向和来源身份，平行路线独立保留。数值明确的时间转为小时，手续原文保留；未知不展示为瞬时。显隐、发现状态与通行规则在持久化和上下文传递中保留，客观隐藏路线不会直接变为 Writer 已知路线。

验证发现并修复了局部子图经其他区域连通被过早拒绝、单向图受节点排序影响的边界。全图结构校验在实际跨区路线入库后进行，失败回滚同一 savepoint；弱连通性不改变实际单向寻路。没有为通过测试增设连接、改变生产数据库或改写旧样本。

相关回归 **78 passed**，包含真实 PostgreSQL 持久化、失败回滚、跨区权限、单向路径、无 ID 平行边、隐藏/未知/裁剪地图与复合时长。独立复审另验证失败扩展在提交并重新打开 Session 后，原地图、Region 和生成记录保持一致。原五项评审问题已收口；后续裁剪图/规则引用问题先复现失败再修复。局部计数互有重叠，不与全量累加。

首次冻结修复提交 `a141d0a3603df6c20b43a01c2b9d279f867ed4c1` 的全量结果为 **1 failed、3104 passed、4 skipped、6 subtests**，354.37 秒。compile/F/E9/源码守卫通过，完整 Ruff 1118、零新增诊断。唯一失败为原有反馈有限闭环测试：夹具把 Mac 的评论时间与 PostgreSQL 虚拟机的 Canon 时间比较，诊断中两者分别为 `08:42:47.599511` 和 `08:42:47.615905`，于是按现有规则正确得到 `feedback_precedes_body`。同一测试另有通过记录，说明不能把它重跑至绿当成消除缺陷。

修正仅在测试的模拟评论输入：从实际 PostgreSQL `clock_timestamp()` 读取同一时钟，同时用于平台产生/观察时间和该次模拟摄入时间。仍执行真实摄入、聚合、计划、Writer、唯一 Canon 和正文观察 owner，不睡眠、不修改正式提交时间、不放松“评论必须在正文之后”的检查。该测试修复后再次冻结全量，首次失败及诊断证据保留；此前准备的 `a141d0a` smoke 没有启动。

## 验证边界与后续

在后续 `03143ef` 的新 Genesis 准备中又发现一个范围较小的字段传递缺陷。该源码已全量 **3123 passed、4 skipped、6 subtests** 并完成两类精确镜像验证，但尚未创建章节任务，因此这里不是新 BODY 失败。

本轮正式 Map 的六条路线用 `access/risk` 保存十二项原文，例如“需入场证、派车单”和“退回医院不等于退回补偿专户”。旧适配入口只读 `control/hazard/access_rule_id`，Genesis 总览只展示耗时，Writer 也未渲染已有 `source_hazard`。独立纯函数回放显示 BookMap、Genesis 总览、Writer 单章/场景输入和主审查均保留 **0/12**，原行程成本则为 **6/6**；泛化的世界权限规则不能替代这些具体条件。

修复使用原导入入口将 `control/access` 与 `hazard/risk` 合并为现有来源 metadata，保留不同的完整原文；Genesis 预览复用同一函数，Writer 展示权限和风险，reviewer 沿原 metadata 链消费。没有新增权限 ID、风险分数、模型调用或门禁，隐藏路线不直接变为 Writer 已知路线。局部和跨区路线提交后重新打开 PostgreSQL Session 验证原文仍在；五种最终写作提示及客观隐藏地图审查也有回归。

十项针对正确协议对象的断言先复现失败，修复后本文件 **52 passed**；相关地图、Genesis、review、retrieval 与 prompt 组合 **192 passed**，计数包含这些局部用例。相同实际 Map、相同回放脚本的 after 结果在五层均为 **12/12**，耗时仍为 **6/6**。这些是输入完整性证据，不能证明模型一定遵守。该运行留在写作前，旧源码、原 Map、before/after 和完整镜像证明保留；后续完整 QA、镜像和新 smoke 以 Task 13 的实际记录为准。

纯函数回放没有新调用 LLM，也没有把原候选重新接纳。确定性移动检查依赖可解析的结构化位置，不能证明自由文本最终 BODY 的所有移动；新增地图证据能否使真实 Writer/主 reviewer 正确执行，仍需新样本验证。手续等待没有转换成凭空确定的数值下限。

本次修复须从新提交冻结全量 QA、构建两种精确镜像，再进行新的 smoke 和独立 L100。旧运行不得恢复后冒称新候选通过，不调整质量门、修复预算或手工接纳来消除失败。私有原始证据保存在对应 run 的 `map-diagnosis/` 及 terminal 导出中，公共报告仅包含脱敏身份与有限结论。
