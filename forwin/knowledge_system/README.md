# Knowledge System

Status: disposable projection and retrieval domain.

This package owns BookState-backed prompt context, Obsidian projection pages,
projection refresh jobs, and projection storage helpers. It never decides or
writes accepted chapter canon.
