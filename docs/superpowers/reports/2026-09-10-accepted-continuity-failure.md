# 已接纳正文的连续性失败与角色修补收敛

## 结论

最新实跑候选 `2df4f4f` 的完整工程回归为 3343 passed / 4 skipped / 6 subtests，且完整 runtime/browser 镜像验证通过。全新 smoke 的第 2–3 章仍有核心证物衔接缺口，安全暂停时正式接纳 1–4 章、active count 为 0。原始抽取已保留的状态还暴露出可复现的 BookState 覆盖缺陷，Task 21 修复该状态更新边界。以下先保留早期证据，最新样本见文末；独立 L100 和真实结局未通过。

`00610f213da6fb18c18a6c9887af054536a9dcf9` 的新 smoke 接纳第 1–3 章后，独立正文审读发现跨章事实冲突；任务在第 4 章的安全检查点暂停，正式章节仍为 1–3。这轮未通过 smoke20，尚未启动合格的独立 L100。

角色称呼另有可复现的代码缺陷，已修复并完成独立审查，提交为 `8f1f296`。后续历史预算、主评审摘要及 Writer 世界页的输入修复已在 Task 18 提交。这些接线修复不能被当作跨章时间线和证据载体问题也已解决。

## 冻结样本与证据

| 项目 | 身份 / 结果 |
| --- | --- |
| 源码树 | `1f68c24c42845d47be3fb6303d4fbc05b157a863` |
| 全量工程测试 | 3240 passed、4 skipped、6 subtests，345.55 秒 |
| runtime 镜像 | `sha256:3be8cb8013013e1afe975b37d4c458170bf88494a4200af96642d6cb6f7a60cb` |
| browser 镜像 | `sha256:d1097033d4bcc0897cb3920a52affe14e8c72e060439cb35e0904ccbfd172c8c` |
| 项目 | `3b3920de3cce4e46ab4d1989f378fc67` |
| 任务 | `b384b932fa4a`，`soak_test`、隔离、目标第 20 章 |
| Genesis / policy | 六阶段锁定 revision 14；policy version 2，仅变更已批准的三个 pause/delegate 字段 |
| 终态 | `paused`；accepted 1–3；canonical active generation count 为 0 |

本轮实际 Map 输出包含全部六字段，七个节点和七条路线通过共享导入校验，来源端点、时长、条件和风险保留。写前唯一局部修订是清空陆衡未定义的组织归属，其他包内容逐项未变。这些写前验证没有被扩张为正文质量保证。

本地证据根目录为 `.superpowers/sdd/2026-09-09-forwin-three-stage/runs/forwin-stage1-smoke-00610f213da6-0b803d/`。`47-chapter-000*.json` 是 canonical 正式正文；`50-body-continuity-assessment.json` 绑定引用、active commit 和正文 SHA；`observer-terminal.json` 证明三章导出身份完整、期间 book revision 一致。106 个 Writer 请求、响应、正文及供应商失败产物按 canonical event URI 只读保存，远端、本地与事件记录的 SHA 一致。

观察器收集 284 条去重事件，29 个窗口达到 200 条上限，因此历史完整性只记为 `sampled_recent_windows_only`，不宣称完整 trace。Kimi 返回额度不足、DeepSeek 返回余额不足，既有路由随后使用 gpt-5.6-sol 完成正文；这是实际备用路由行为，不能写成全程由首选供应商成功生成。

## 正文问题

1. **载体改变。** 第 1 章从推车取出厚装订往来账，明确核见连续原页与完整骑缝章；紧接的第 2 章却说同一份往来账没有成册账本，只有电子导出记录。没有交代换源、发现另一份记录或更正前文。
2. **时间顺序反转。** 第 1 章记载审批日期为六月二十八日、退回登记为七月二日，且“已付”入账在前；第 2 章继续沿先付后退的记录顺序核查。第 3 章把同一退回时间写成早于付款审批，用它构造新的异常，却未区分另一个时间字段或提供纠正来源。
3. **进展重复与元叙述。** 第 2 章重新确认第 1 章已经确认的同一笔补偿及三种矛盾状态，并出现“前一章”字样。问题不只是章节计数或状态表能否继续推进。

三章都以第一个候选进入正式版本，未经历重写，主 review 为 warn、Canon 没有记录阻断问题，也未人工强制放行。下一步须追查输入覆盖和判断依据，不能以新增同款小说的专用规则补齐一个样本。

## 已收敛的角色修补

原始第 1 章模型 BODY 有 17 处“工作人员”，没有“具体见证人”。旧检测器因“工作人员停下贴封条的手”判定占位泄漏；随后 `apply_placeholder_leakage_autofix` 全局替换。原始 BODY 逐字替换这 17 处，恰好等于接纳 BODY，证明这一问题来自代码变换。

现在删除该自动替换及实际调用方，不再替未确定身份编造别名。普通动作、并列岗位、徽章和独立一行岗位名不构成确定的姓名占位证据。明确姓名、签名或署名栏的占位值、以占位替代预期主角、内部状态键仍保留原有检查；真正需要语义修复时交给已有 Writer 与有限预算。局部修复没有确定性替换时返回 `needs_writer`，不会把不变正文报告成一次成功重写。已核实的 Canon 人名修正及重新评审、候选关联和事务回滚保持。

回归先复现 8 失败 / 2 通过；独立审查补充署名位置、值边界、岗位并列和局部修复空转的失败样本。最终根代理 103 项相关测试通过，独立 87 项及 3 项姓名修正专测通过（有重叠，不相加）。真实原始 BODY 的纯函数重放也确认误报消失，原产物 SHA 未变；没有模型或数据库调用。

## 历史信息传递的诊断与修复范围

独立纯函数对照只增加 65,000 字符的不可见 `knowledge_system_context`，五种 Writer 模式的实际提示原本完全相同，预算估算却从 2484 增至 67551；在 6000 字预算下，两条摘要被裁成一条、两条 memory 全部删除，裁后仍为 67292。这证明溯源副本错误参与裁剪。实际第 3 章记录也是两摘要/两记忆变为一摘要/零记忆，但没有完整裁剪前 pack，不能把实际 92788 字估算全部归到这个字段。

主 LLM Reviewer 的 context builder 已经传入前章摘要，最终 payload 却完全不用；替换全部摘要后 payload 仍逐项相等。Writer 世界页则主动截取 `Current State` 之前的文字，正常导出页的长 frontmatter 挤掉正文；单章与预演模式原先完全未调用世界页渲染。这三处是一般性的代码接线缺口。

Task 18 沿现有链修复：仅从 Writer 预算排除不可见溯源，context 原记录不变；主 reviewer 获取全部已保留摘要和可引用的输入序号；五模式共用可见世界页的概要，具备来源的单个实体页还读取当前状态，沿用每页有界内容和既有显隐规则。真假关系保留为限定信息，手工笔记、拟议修改和隐藏真相不进入页面正文输入。没有提高预算、新建 reviewer、扩展 memory 存储或添加本书专用规则。

首次三项有效失败回归验证预算驱逐与 reviewer 漏接，修复后通过；世界页的 38 项失败与 3 项通过随后变为 41 项全部通过，涵盖五模式、真假/显隐、metadata 与正文不一致及输入不变性。较早两次测试有 fixture 必填字段遗漏，日志保留，未算作有效 bug 复现。

独立审查进一步复现了源节点标记丢失：真实 Obsidian exporter 把隐藏人物导出为 reader_known/canon_live，新读取 Current State 会泄露状态。这一轮没有进入验收运行。补充的源导出和旧页面读侧 10 项失败、历史/地图/聚合页 4 项失败已修复：隐藏节点判断收为唯一 BookState owner，导出保留其显隐；已有页面按请求章号的 runtime 复核，不修改已存投影。未来页面先排除再排序，缺少实际实体来源时保持隐藏；地图与书籍合成 ID 不误判为世界节点。书籍/overview 等混合显隐聚合页只保留概要，Current State 限具备来源身份的单实体页。读侧也复用隐藏 truth_relation 判断，新增两项失败回归已修复。最终合同 61 项、相关组合 273 项通过；独立复审 84 项与两项原始隐藏真相探针通过，本补丁范围没有剩余问题。这些测试计数有重叠，不是额外小说样本。

仍须明确：第 1 章的审批日期、退回日期、纸本及骑缝章细节位于正文第 1225/1451/1260/1550 字符附近（零基偏移），既不在正式摘要，也不在已存 memory 的前 500 字内。第 2 章计划原文还要求呈现“纸本、电子台账、代发清单”的差异，与已接纳正文存在张力。因此补回现有摘要及页面不等于恢复全部事实，更不等于模型会使用证据或评审必能发现冲突。

Task 18 提交 `7ddeffd` 的冻结 suite 为 3322 passed、4 skipped、6 subtests，编译和关键静态检查通过；整体检查因新增一项 Ruff I001 未通过，没有构建镜像或启动准备好的 smoke。后续 `1614560` 仅调整 import 排版，验证前后 Python AST 完全相同，再通过全部工程和两类镜像检查。原失败记录保留。

## 后续 `1614560`：编号线索与分场信息传递

新项目 `953a1b4de6074f73af0b33a4d4ee992d`、任务 `abfa6531abc5` 使用新生成并锁定的 Genesis revision 17、冻结 policy version 2。第 1 章正式 BODY 同时出现“项目编码没有一个字符相同。”与“剩下的六位却完全相同。”，却没有解释为不同字段或修正先前观察。这个直接矛盾位于推动调查的核心线索中。

第 2 章将共同外包项目编号写为 `WB17`。把年份 `21` 与 `WB17` 拼成六位是可能的解释，但正文明确分列年份和项目号，没有给出对应关系。因此它是需要解释的连续性缺口，证据强度弱于第 1 章的直接矛盾。四箱查阅有明确补充授权，台账页与三箱原始凭证也可区分；不将这些或模糊的封柜阶段描述额外计作已证明的严重错误。

| 正式章 | active commit | BODY SHA-256 |
| --- | --- | --- |
| 1 / v1 | `b8fda933caab8bc3194eed9c5f680f49b82754ec0ed25cb9524b344022cec0bb` | `4bc5dd0f921daf60518169067e693becaa793b7f5f19a8c400e371a61683990e` |
| 2 / v2 | `f64a9041b2de5bf2d9c379db02a806e689d5256082f8d227355f394d9b7b7367` | `0322c1b02f63394fc64fc4cc84670f28823db067666d9b27dd03cc31423e0460` |

已通过 canonical `task_pause` 安全暂停；原有限观察器正常结束，接受范围 `[1,2]`、导出期间 book revision 稳定，canonical active count 为 0。未接受的第 3 章不能计入完成。没有继续旧任务、手动放行或修改正式 BODY。100 个原始请求/响应核对 event/remote/local SHA，另六个 WriterOutput/正文文件核对 remote/local SHA；主评审与修复核验缺少 18 项原始请求/响应引用，事件窗口也有缺口，不能宣称掌握完整模型链日志。

原始 stitch 经实际 parser 解析后，与 WriterOutput 和正式 BODY 完全同 hash。第 1 场生成“无共同字符”，第 2 场选择共同中段，第 3 场选择相同末六位；各场调用只有自身计划，没有已生成前场正文。stitch 收到了冲突，却保留了两种说法。第 2 章修复的分场也选择了不同月份，stitch 统一成八月，说明它有部分纠错价值。主 reviewer 的完整原始输入缺失，不能进一步断言某个具体评审环节的漏检原因。

Task 19 保留唯一 Scene Writer，补齐本章计划与已生成前稿的有界传递。首次针对传递、隔离及预算的测试为 5 failed / 1 passed，修复后 9 项合同测试与 162 项相关组合通过；计数重叠。后加 fallback 测试最初误设默认 trace stage，修正测试显式使用 `chapter_draft` 后通过，没有为该夹具修改生产行为。原始 scene 的纯输入增量重放涵盖三次 Writer 执行、九个 scene 请求，新增 1213–2992 字符；前场正文均完整保留，第三场额外计划各省略两条且明确标记，当前分场自身任务始终单独保留。完整源 scene 元数据经过新旧 stitch renderer 的结果一致。

新增段的预算是现有章节最大字数，优先保留连续最近前稿，再附完整计划；未附原文不冒充完整历史。没有增加模型调用，但不代表输入 token 或费用不增加。该重放没有重建缺失的完整原始 ContextPack，没有调用真实模型或证明新正文质量。第 1 章六位长度仍不在摘要或 memory 前 500 字内，场间传递不能补回这项跨章事实。

独立实现复审通过 97 项相关测试（含九项新合同测试），另执行 400 项预算/省略/完整记录探针及非单调场景编号的真实 Writer 顺序检查，未发现本补丁阻断问题。报告保存在私有审查目录 `review/task19-handoff-review.md`。

## Task 20：世界页可见内容的预算修复

独立基线另确认世界页中不渲染的手工笔记及分类副本仍会计入预算、挤掉可见历史。真实 typed context 的同一可见输入，加入不可见内容后估算从 2836 增为 72829；固定预算 2836 时，摘要从两条减为一条，记忆从一条减为零。原始 runtime pack 缺失，因此这是单独复现的估算缺陷，不能认定它导致了上述编号错误。

Task 20 把既有世界页 renderer 原样移入一个叶模块，由五种 Writer 提示及两处预算估算共用。预算只计渲染结果一次；完整页面、人工笔记、隐藏信息和来源 metadata 仍在原 context 中供 reviewer 使用，没有修改页面或 Canon。预算阈值、裁剪顺序及空世界的既有固定开销不变。这仍是字符估算，不能作为精确 token 表计。

最初回归为 6 failed / 5 passed。第一版实现遗漏了空世界组件开销，新增回归明确复现 1870 与原 2130 不一致，修正后才进入独立审查。最终十二项合同测试、根代理 188 项相关及 37 项额外回归通过；独立复审 130 项相关测试、20 组空世界前后估算对照、实际 reviewer 完整证据保留和三种冷导入顺序检查通过，无阻断问题。测试计数有重叠。

原样渲染经 AST 及八组前后文本对照核实；同一 typed fixture 在原预算 2836 下，两组估算均为 2193，均保留两条摘要和一条记忆，五模式可见提示相同。私有证据为 `review/task20-before.json`、`task20-after.json`、`task20-world-budget-review.md`。未调用真实模型，也不能补回从未进入摘要或 memory 的六位编号细节。

Task 19 与 Task 20 的组合源码 `2df4f4f` 已通过下述工程验证，但新 smoke 未通过。当前 smoke20、独立 L100 与真实结局仍未通过。

## 后续 `2df4f4f`：证物衔接与当前状态被旧副本覆盖

冻结树 `8276de73945e8c590afdc811135904fb5899fb31` 通过 3343 项测试、4 项跳过、6 subtests；runtime `f62702926758…` 和 browser `d38787309544…` 均完成精确源码与角色验证。新项目 `f0215ec406d04cfb923cd64a07364e6f`、任务 `7e90d9620ada` 使用锁定 Genesis revision 16 和 policy version 2。原单一观察器正常结束，四章正文 hash 与导出身份一致，导出期间 book revision 稳定；第 5 章未接纳，未恢复失败任务。

第 2 章的档袋目录为三页，现存第一页拨付指令、第二页退件处理，第三页银行回执缺失；两人将其封存并签署骑缝。第 3 章直接查验同案卷宗，目录变为核定表、拨付通知、银行回执、内部交接记录四项，未说明另取卷宗、两套目录关系或拆封手续。主卷和附件袋可以不同，因此不能只用“三与四”证明矛盾；实际重要缺陷是核心物证的身份与保管衔接无法从正文追溯。两次跨地行程符合锁定路线，12:44 的章首重叠及局部推断过强分别只是次要问题。

102 个原始请求/响应匹配 event/remote/local SHA，另六个 WriterOutput/正文文件匹配 remote/local SHA，后者的事件没有提供文件 hash。终态观察器保留 415 个去重事件、92 个饱和窗口，仅代表采样历史；前三章还缺少六项成功调用的原始引用，不能宣称掌握主 BODY reviewer 的完整输入。独立报告为私有 `review/task21-accepted-continuity-baseline.md`。

实际第 2 章 state/lore 抽取及保存的 WriterOutput 已包含“暂停流转并就地封存”“现存两页且缺银行回执”。第 3 章的分解与首场输入没有这些细节；同章后续场景则完整收到已有前场正文，Task 19 的实际传递正常。canonical `world_model_get(as_of_chapter=2)` 返回的快照与第 3 章提示所引快照身份相同：相关事件保留三页/两页和封存，但档袋状态却为 `active`。

沿原始两次更新做离线重放，`state.status` 先得到封存，随后 `metadata.writer_state.contents` 更新将其恢复为 `active`。原因是 runtime 只更新状态索引，非状态补丁随后从节点的旧状态副本重建对象并覆盖索引。此 owner 在 `1614560` 与 `2df4f4f` 间没有变化，不是已证明的 Task 19/20 回归。

Task 21 让非状态更新基于当前索引，并沿既有合法整段 `state` 更新补齐 before-image、状态行、空快照覆盖和修订来源核验。没有新增状态仓库、模型调用或题材专用规则，没有改写旧快照。八项 runtime 回归先出现七项失败、一项原有控制通过；既有 Writer 合同测试扩展到 gate→compile→snapshot→reload 后也在旧实现明确失败。整段状态与空快照的五项失败、修订来源的一项失败及元数据来源拒绝控制分别留证。相关旧测试原先把初始位置写在第 1 章，再从第 0 章编译第 1 章快照，依赖空快照被忽略；现将初始状态放回第 0 章，所有行为断言保留。

这个修复证明当前状态不会被无关字段更新撤销，不能证明正文衔接已经修好。`contents` 仍位于结构化 metadata，检索选页与 Writer 有界渲染是否提供足够证据仍待核查；没有拿缺失的原始 ContextPack 推断具体裁剪原因。旧失败样本、实际费用及缺失证据均保留，新烟测、独立百章和结局尚待完成。

最终根代理相关组合 143 项、更大范围 BookState/Canon/历史修订 486 项通过；独立 runtime 13 项及旧值拒绝、空快照、错误来源探针通过，计数有重叠。四个生产文件的审查 hash 与被测文件一致，独立审查无阻断发现。此处不是新源码的全量 QA、镜像或真实正文通过记录。
